{
    this.props.tracks.map((track) => {
    return <Track track={track} key={track.id}/>;
    })
}

{this.props.track.artist}

  /* {
                    this.props.tracks.map((track) => {
                    return <Track track={track} key={track.id}/>;
                    })
                } */